<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="add_crm_real_estate.list">
      <field name="name">add_crm_real_estate list</field>
      <field name="model">add_crm_real_estate.add_crm_real_estate</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="add_crm_real_estate.action_window">
      <field name="name">add_crm_real_estate window</field>
      <field name="res_model">add_crm_real_estate.add_crm_real_estate</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="add_crm_real_estate.action_server">
      <field name="name">add_crm_real_estate server</field>
      <field name="model_id" ref="model_add_crm_real_estate_add_crm_real_estate"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="add_crm_real_estate" id="add_crm_real_estate.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="add_crm_real_estate.menu_1" parent="add_crm_real_estate.menu_root"/>
    <menuitem name="Menu 2" id="add_crm_real_estate.menu_2" parent="add_crm_real_estate.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="add_crm_real_estate.menu_1_list" parent="add_crm_real_estate.menu_1"
              action="add_crm_real_estate.action_window"/>
    <menuitem name="Server to list" id="add_crm_real_estate" parent="add_crm_real_estate.menu_2"
              action="add_crm_real_estate.action_server"/>
    -->
  </data>
</odoo>